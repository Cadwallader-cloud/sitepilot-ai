/**
 * Variant System — typed presets for UI Kit components.
 */

export { BUTTON_VARIANTS, type ButtonVariant } from "@/components/ui/button-variants";

export const VARIANT_SYSTEM_RULE =
  "Components expose variant prop backed by a typed preset map — no one-off styles." as const;
